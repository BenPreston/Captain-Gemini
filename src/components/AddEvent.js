import React from 'react'

// This will need to connect directly to the firebase database to add new events. 
// It needs parameters of type, detail, boat, enquire
export default function AddEvent() {
    return (
        <div>
            <h1>Add a event</h1>
        </div>
    )
}
